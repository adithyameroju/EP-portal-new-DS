import { Meta } from "@storybook/addon-docs/blocks";
import CompassSop from "./CompassSop";

{/* The Compass SOP — the designer front door. Rendered as a React component (not the
    injected-HTML pattern) so the Copy/Download buttons work inside Storybook. Same
    content as the standalone compass-sop.html; shared assets in ./sop-assets. */}

<Meta title="Introduction/Working with Compass/Compass SOP" />

<CompassSop />
